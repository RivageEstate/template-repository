name: CI

on:
  pull_request:
  push:
    branches: [ main ]

jobs:
  quick-check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run lightweight checks
        run: bash scripts/check.sh
